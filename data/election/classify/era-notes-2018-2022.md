# Era notes — 2018-2022 term (batches 40-65)

Gate round 10 fixer notes: three verified opus-gate findings, all corrected
via `corrections.json` (batch files untouched).

## 0. Downtown Loop group: consulting-engineer appointment vs tender award

`8dc6de530d39` (2020-01-14 Council item 8.3.10, AECOM appointed Consulting
Engineer for the Downtown Loop project under s.15.2(e) procurement policy)
had already been moved `issue: downtown -> transit`, `axis:
application-approval -> transit-service` by an earlier BRT-consistency fix,
but `polarity: expansive` was left standing -- so this pure procurement
appointment (full motion text, parts a-e, is appointment/financing/
administrative-execution only, no transit-service content) was tallying Van
Meerbergen and van Holst as opponents of "expanded public transit service"
when they voted against a consulting-engineer hire. Corrected `axis` and
`polarity` to `null`, matching its structural twins `b5e77870e85f`
(DILLON/East London Link) and `96a8316ca83a` (AECOM Wellington Gateway),
both already `axis: null`/`polarity: null` for the identical pattern.

The two Downtown Loop **tender awards** in the same item family --
`2cb6c9292ef6` (Council ratification) and `74f91fe1f257` (Civic Works
Committee recommendation), both accepting L82 Construction Ltd.'s
construction bid -- were checked against the same question and confirmed to
**genuinely differ**: a construction-tender bid acceptance is a real capital
commitment to build the project, not a directionless procurement-
appointment step. This matches corpus precedent `4214b0fc1e5c` and
`6dd7c2a2b04d` (other transit contract-award bid acceptances, kept `axis:
application-approval`/`polarity: expansive`). Left unchanged.

All four Downtown Loop correction rows (`0dd0f857160d`, `2cb6c9292ef6`,
`74f91fe1f257`, `8dc6de530d39`) had their `reason` fields amended
(append-only) with an explicit supersession notice, because their batch
verifierNotes still assert claims that are now stale post-correction --
`8dc6de530d39`'s verifierNote still reads "issue=downtown is a legitimate
keyword hit"; `74f91fe1f257`'s still reads "application-approval is the
only axis available" -- both describing the PRE-correction batch state. An
auditor reading only the batch file would see a stale rationale; the
corrections row is now explicit that it supersedes it.

## HDC dissolution vote (1de168466ef0)

`1de168466ef0` (2020-12-16 Strategic Priorities and Policy Committee item
4.1, dissolving Housing Development Corporation, London and integrating its
portfolio into the City's corporate structure, 12-3) was `issue: housing`,
`axis: application-approval`, `polarity: expansive` -- tallying Helmer,
Turner and Kayabaga as voting against housing. Full motion text (parts a-d)
is entirely a governance/corporate-restructuring decision with no clause
about expanding or restricting housing supply or policy; the three
councillors opposed dissolving a corporate entity, not affordable housing.
Corrected `axis` and `polarity` to `null`; `issue: housing` is left as-is
(the keyword hit -- "affordable housing portfolio" -- is a direct textual
match and the row's own `whatAYeaDid` sentence remains accurate; only the
axis/polarity aggregate tally was false).

---

Gate round 9 fixer notes: two items in the round's brief required no
correction, but are recorded here (and in the PR body) so the reasoning
isn't lost.

## 1. Career-layer axis-label adjacency (3cacd5dd213d)

`3cacd5dd213d` (2019-06-18 Corporate Services Committee item 2.8, Wharncliffe
Road South expropriation, 5-1) renders "expanded general-purpose road
capacity" under `issue: transit`, `axis: road-capacity`. That's correct as
classified — road-capacity is one of the two axes under the `transit` issue
cluster (see `direction-rules.ts`'s 2026-08-31 transit/road-capacity split:
transit-service for bus/BRT content, road-capacity for general-purpose lane
widenings, both filed under the `transit` issue umbrella) — but on a career
page that shows a councillor's votes across MULTIPLE terms/eras, the label
"expanded general-purpose road capacity" needs to stay visually adjacent to
its axis context so a reader doesn't misread a road-widening vote as a
transit-SERVICE vote. This is a **design requirement for the career-layer
renderer**, not a data defect in this entry — noted for whoever builds or
reviews that renderer, not fixed here.

## 2. Percent-levy vote carries no direction (272eaf831e6f)

`272eaf831e6f` (2019-05-21 Council item 8.4.5, 10-4) is the era's most
quotable levy vote — a 2020-2023 multi-year planning target of "approximately
2.7%" average annual tax levy increase — and it is currently `axis: null`,
`polarity: null` (issue=budget, no direction). This is **rule-correct, not a
gap**: the levy-size axis's expansive/restrictive rules in `direction-rules.ts`
are keyed to a **dollar** figure (a `$` levy-effect line), and this clause's
own operative text prints a **percentage** target, not a dollar amount, so
no direction is mechanically derivable from it under the axis's current
rule. Extending the levy-size axis (or adding a new axis) to also derive a
direction from a percent-target clause is a real, deliberate rule change —
worth doing, since it's the term's single most quotable levy vote — but it's
a decision for the humans reviewing this gate, not something a fixer should
silently invent a new derivation rule for. Left `unclear` as the rules
currently stand.
