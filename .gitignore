node_modules/
public/
.quartz-cache/
tsconfig.tsbuildinfo
__pycache__/

# Vector database - regenerate on deploy, don't persist stale data
lancedb/

# Chat logs - contain user data, should not be committed
server/chat-logs/.DS_Store

# Environment variables
.env
.env.local
.env.production
.DS_Store
*.sync-conflict-*

# Recovery-scrape run log - regenerated every run, not a durable artifact
# (the checkpoint manifest.json alongside it IS committed - that's the
# durable resume state; this is just the run's console transcript)
scraping/recovery_checkpoint/run.log
